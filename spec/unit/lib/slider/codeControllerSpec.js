describe("code controller", function() {
  beforeEach(module('Slider'));

    beforeEach(inject(function($compile, $rootScope, $httpBackend) {
      this.compile = $compile;
      this.scope = $rootScope.$new();
    }));
  
});
